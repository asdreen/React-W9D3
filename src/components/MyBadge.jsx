import { Button, Badge } from "react-bootstrap";

function MyBadge() {
  return (
    <Button variant="warning">
      Badge <Badge bg="warning"></Badge>
    </Button>
  );
}

export default MyBadge;
